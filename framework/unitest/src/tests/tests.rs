// use crate::testing::{must, test, unit};

// unit!(
//   test!(integration_test_number_should_be_eq, must!(eq: 0, 0));
//   test!(integration_test_numeber_should_be_ne, must!(ne: 1, 0));
//   test!(integration_test_true_shoud_be_truthy, must!(truthy: true));
//   test!(integration_test_false_should_be_falsy, must!(falsy: false));
//   test!(integration_test_panic_should_be_catched, must!(die: { panic!() } ));
// );
